# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
# set $term2 xfce4-terminal -x fish
#set $term2 alacritty -e tmux
#set $term alacritty 
set $term xfce4-terminal
set $term2 xfce4-terminal -x fish
set $world firefox --private-window
#set $world brave
#set $world2 chromium

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font pango:monospace 8
# font pango:Fira Code Medium:style=bold 10
#font pango:FiraCode Nerd Font Med:style=bold 10
# font pango:Fira Code Medium:style=bold 10
# font pango:JetBrainsMono NFM Medium 10
# font pango:JetBrainsMono Medium 10
font pango:JetBrains Mono Medium 10
# font pango:hack 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
#set $refresh_i3status killall -SIGUSR1 i3status
#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
#bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
#bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec $term
bindsym $mod+z exec $term2
#bindsym $mod+z exec $term -x sh
#bindsym $mod+z exec $term -e sh
#bindsym $mod+q exec $term --drop-down
bindsym $mod+q exec qjackctl
bindsym $mod+b exec $world
#bindsym $mod+c exec $world2
bindsym $mod+t exec --no-startup-id ~/.scripts/tele
bindsym $mod+m exec scide
bindsym $mod+n exec thunar
bindsym $mod+p exec $term -x python
#bindsym $mod+g exec $term -e vim
#bindsym $mod+g exec $term2 -x vim
bindsym $mod+g exec $term -x vim
bindsym $mod+Shift+x exec systemctl poweroff
bindsym $mod+Shift+z exec systemctl reboot

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec --no-startup-id dmenu_run
#bindsym $mod+d exec --no-startup-id dmenu_run -nb '#1d1f21' -sb '#5f819d' -l 20 -fn 'Hack 10'
#### bindsym $mod+d exec --no-startup-id dmenu_run -nb '#1d1f21' -sb '#5f819d' -l 20 -fn 'Fira Code Medium 21'
bindsym $mod+d exec --no-startup-id dmenu_run -nb '#1d1f21' -sb '#5f819d' -l 10 -fn 'JetBrains Mono SemiBold-10'
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec rofi -modi drun,run -show drun
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
#bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

## Start i3bar to display a workspace bar (plus the system information i3status
## finds out, if available)      [dal 201 al 221] (comment 201,221s/^/#) (decomment 201,221s/^#)
#bar {
###        status_command i3status
#        status_command i3status -c ~/.config/i3/i3status.conf
###        tray_output primary
##        status_command i3blocks -c ~/.config/i3/i3blocks.conf
###        font pango:Inconsolata Medium 11
##        font pango:Hack Bold 10
#        font pango:JetBrainsMono NFM Bold 10
###        font pango:Cantarell Bold 10
##        font pango:Fira Code Medium 12
###        font pango:Monospace Bold 21
#        position top
#        colors {
#           background #2F2F2F
#           statusline #eeeeee
#           separator #666666
#           #                  border  backgr. text
#           focused_workspace  #474747 #789f77 #474747
#           active_workspace   #a9a9a9 #222222 #a9a9a9
#           inactive_workspace #a9a9a9 #222222 #a9a9a9
#           urgent_workspace   #4A4A4A #4A4A4A #a9a9a9
#        }
#}
#
#
#
#
# for_window [class="^.*"] border pixel 10
# for_window [class="^.*"] floating enable
for_window [class=".*"] floating enable
# for_window [title="xclock"] floating enable
# for_window [class="XTerm"] floating enable
# for_window [class="alacritty"] floating enable
# for_window [class="Terminal"] floating enable
# default_border pixel 0
exec feh --bg-scale ~/ArchLinux/Immagini/arch.png &
#exec feh --bg-scale ~/Immagini/cli.jpg &
#exec feh --bg-scale ~/Immagini/fol.jpg &
#exec feh --bg-scale ~/Immagini/1B1B1B-hex.png &
#exec feh --bg-scale ~/Immagini/17191c.png &
#exec feh --bg-scale ~/Immagini/0d1114.png &
#exec feh --bg-scale ~/Immagini/lake.jpg &
##exec feh --bg-scale ~/Immagini/Blue-Wallpaper-For-Background-1.jpg &
#exec feh --bg-scale ~/Immagini/folife.png &
#     exec redshift -l 44.494888:11.342616 -t 6500:3600 &
exec redshift -l 43.3:13.7 -t 6500:3600 &
exec xset dpms 60
# exec xset s off -dpms
#exec feh --bg-scale ~/Downloads/ice.jpg &
#exec feh --bg-scale ~/pic/gen.jpg &
exec xsetbg Immagini/mountain.jpg
exec setxkbmap it
####################################################################################set $Locker i3lock && sleep 1

#set $mode_system System (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
set $mode_system System (e) logout, (r) reboot, (Shift+s) shutdown --> Ciao Genny       !!!
mode "$mode_system" {
#    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
#    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
#    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"  

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Pause mode "$mode_system"
bindsym $mod+y exec qjackctl
#bindsym $mod+u exec $term -e ncmpcpp
####################################SCREENSHOTS####################################
bindsym $mod+Print exec \
     scrot -q 100 '%F_%H-%M-%S.png' -e 'mv $f ~/Immagini/screenshots/'
bindsym $mod+Shift+Print exec \
     scrot -u -q 100 '%F_%H-%M-%S.png' -e 'mv $f ~/Immagini/screenshots/'
bindsym $mod+Ctrl+Print exec \
     scrot -s -q 100 '%F_%H-%M-%S.png' -e 'mv $f ~/Immagini/screenshots/'
################################### AUDIO

#exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec_always --no-startup-id killall polybar; polybar example
# exec_always --no-startup-id polybar example
##exec_always --no-startup-id polybar second
exec_always setxkbmap it
#exec_always "setxkbmap -model pc105 -layout it -variant ,qwerty"
####exec_always "setxkbmap -layout us -variant altgr-intl"
#exec_always --no-startup-id 
#exec_always --no-startup-id killall lemonbar

#exec sleep 1s && bash ~/.config/lemonbar/lemonbar.sh | lemonbar -p -F#FFFFFFFF -B#FF222222 -f "DejaVu Sans Mono"-8 -f FontAwesome-8 &
####exec sleep 1s && bash ~/.config/lemonbar/lemonbar.sh | lemonbar -p -F#FFFFFFFF -B#FF222222 -f "Hack"-10 -f FontAwesome-8 &
# Alsa
#bindsym $mod+u exec amixer set Master toggle
#bindsym $mod+i exec amixer -c 0 set Master -q 5%-
#bindsym $mod+o exec amixer -c 0 set Master -q 5%+
# Pulseaudio 
#bindsym $mod+i exec amixer set Master -q 5%-
#bindsym $mod+o exec amixer set Master -q 5%+
#bindsym $mod+u exec amixer set Master toggle
#######################################################################################
#
# Black background with arch blue text and borders
# grey/white background - blue text for rest
# Windows decoration colors
# class border backgr. text indicator
client.placeholder #242424 #242424 #242424
client.background #242424 #242424 #242424
client.focused #4A4A4A #4A4A4A #e5e5e5 #4A4A4A
client.unfocused #4A4A4A #222222 #aaaaaa #222222
client.focused_inactive #4A4A4A #222222 #a9a9a9 #222222
client.urgent #4A4A4A #4A4A4A #f7f7f7 #4A4A4A

# exec_always picom --blur-background --blur-strength 6 --shadow-color color -cC & 
######################### exec_always picom --config ~/.config/picom/picom.conf &
# exec_always picom --config ~/.config/picom/picom.conf.example &
#### exec_always picom --config ~/.config/picom/examples/picom.sample.conf &
#
#
#
#
##exec wal -i pic/onda.jpg &
#exec wal -i pic/gor.jpg &
#exec wal -i pic/gen.jpg &

####exec volumeicon &

# change volume or toggle mute
#bindsym XF86AudioRaiseVolume exec amixer -q -D pulse sset Master 5%+ && pkill -RTMIN+10 i3blocks 
#bindsym XF86AudioLowerVolume exec amixer -q -D pulse sset Master 5%- && pkill -RTMIN+10 i3blocks
#bindsym XF86AudioMute exec amixer -q -D pulse sset Master toggle && pkill -RTMIN+10 i3blocks

#bindsym $mod+i exec amixer set Master -q 5%- && pkill -RTMIN+10 i3blocks
#bindsym $mod+o exec amixer set Master -q 5%+ && pkill -RTMIN+10 i3blocks

## Pulse Audio controls
#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl -- set-sink-volume 0 +5% #increase sound volume
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl -- set-sink-volume 0 -5% #decrease sound volume
#bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound
##https://unix.stackexchange.com/questions/439486/how-can-i-make-media-keys-work-with-i3
#
bindsym $mod+i exec --no-startup-id pactl -- set-sink-volume 0 -5%
bindsym $mod+o exec --no-startup-id pactl -- set-sink-volume 0 +5%
bindsym $mod+u exec --no-startup-id pactl set-sink-mute 0 toggle


#bindsym $mod+i exec --no-startup-id amixer -c 1 set Master -q -5%
#bindsym $mod+o exec --no-startup-id amixer -c 1 set Master -q +5%
#bindsym $mod+u exec --no-startup-id amixer -q sset Master toggle

#bindsym $mod+i exec --no-startup-id pactl -- set-sink-volume @DEFAULT_SINK@ -5%
#bindsym $mod+o exec --no-startup-id pactl -- set-sink-volume @DEFAULT_SINK@ +5%
#bindsym $mod+u exec --no-startup-id pactl set-sink-mute @DEFAULT_SING@ toggle

bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -c 0 set Master -q 5%+ #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -c 0 set Master -q 5%- #decrease sound volume
#bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -c 1 set Master -q 5%+ #increase sound volume
##bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% #increase sound volume
##bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% #decrease sound volume
#bindsym XF86AudioLowerVolume exec --no-startup-id amixer -c 1 set Master -q 5%- #decrease sound volume
#bindsym XF86AudioMute exec --no-startup-id amixer -c 1 set Master 0 toggle
# mute sound
#bindsym XF86AudioMute exec amixer -c 1 set Master 1+ toggle
#bindsym XF86AudioMute exec amixer -c 1 set Master 0+ toggle
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle # mute sound
###bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5%
###bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5%
# https://www.reddit.com/r/i3wm/comments/gyzqbb/how_do_you_control_pulse_audio_with_media_keys/?rdt=38613
#bindsym XF86AudioRaiseVolume exec "pactl set-sink-volume @DEFAULT_SINK@ +1000"
#bindsym XF86AudioLowerVolume  exec "pactl set-sink-volume @DEFAULT_SINK@ -1000"
